
  # Create sidebar with tabs

  This is a code bundle for Create sidebar with tabs. The original project is available at https://www.figma.com/design/vloqnAB0SfNMaDZrxxOZ7V/Create-sidebar-with-tabs.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  